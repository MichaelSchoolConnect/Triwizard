<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lebogang.triwizard">

    <!-- Asking permission to access the internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Asking permission to access the WiFi states -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Asking permission to access the phone's network states -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- SpellsActivity -->
        <activity android:name=".SpellsActivity"
            android:parentActivityName=".MainActivity"
            android:launchMode="singleTop"
            android:label="@string/spells"/>

        <!-- MainActivity -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- HousesActivity -->
        <activity android:name=".HousesActivity"
            android:parentActivityName=".MainActivity"
            android:launchMode="singleTop"
            android:label="@string/houses"/>

    </application>

</manifest>